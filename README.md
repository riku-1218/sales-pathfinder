# Sales-Pathfinder

🎯 **コンセプト**: 「商談前の30分のリサーチを、0秒にする」

営業先企業の課題を自動特定し、自社商材の提案シナリオまで生成する自律型営業リサーチエージェント。

## 🏗 アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│                        入力                                  │
│   company_name / company_url / sales_scenario               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│               Dify Workflow (Orchestration)                 │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐        │
│  │ 経営視点検索  │ │ 現場視点検索  │ │ ニュース検索  │        │
│  │ (中計・IR)   │ │ (採用・口コミ) │ │ (プレスリリース)│        │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘        │
│         │               │               │                  │
│         │      ┌────────┴────────┐       │                  │
│         │      │ GCF HTML Parser │       │                  │
│         │      └────────┬────────┘       │                  │
│         │               │               │                  │
│         └───────────────┼───────────────┘                  │
│                         ▼                                   │
│            ┌───────────────────────┐                        │
│            │   課題仮説の構築      │                        │
│            └───────────┬───────────┘                        │
│                         ▼                                   │
│    ┌────────────────────────────────────────────┐           │
│    │  提案ロジック生成 + 自社商材ナレッジ(RAG)  │           │
│    └───────────────────┬────────────────────────┘           │
└────────────────────────┼────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                 Make - 連携出力                              │
│         Slack / Google Docs / CRM                           │
└─────────────────────────────────────────────────────────────┘
```

## 📁 プロジェクト構成

```
sales-pathfinder/
├── docs/
│   ├── specification.md        # 開発仕様書
│   ├── make_integration.md     # Make連携設計
│   └── templates/
│       └── meeting_prep_sheet.md
├── src/
│   ├── functions/
│   │   └── html_parser/        # GCF: 採用ページパーサー
│   │       ├── main.py
│   │       ├── requirements.txt
│   │       └── test_main.py
│   └── dify-workflows/         # Dify ワークフロー設計
│       ├── main_workflow.md
│       ├── scout_agent.md
│       ├── hypothesis_agent.md
│       └── sales_strategist.md
├── config/
│   └── sample_products.json    # 自社商材サンプル
├── .env.example
└── README.md
```

## 🛠 技術スタック

| レイヤー | 技術 |
|---------|------|
| Orchestration | Dify (Workflow Mode) |
| LLM | Gemini 1.5 Flash |
| Search | Tavily API |
| Serverless | Google Cloud Functions (Python) |
| RAG | Dify Knowledge |
| Integration | Make |

## 🚀 セットアップ

### 1. 環境変数の設定

```bash
cp .env.example .env
# 以下を設定
# TAVILY_API_KEY=your_tavily_api_key
# GCP_PROJECT_ID=your_gcp_project_id
```

### 2. Cloud Functions のデプロイ

```bash
cd src/functions/html_parser
gcloud functions deploy parse-job-posting \
  --runtime python311 \
  --trigger-http \
  --allow-unauthenticated \
  --entry-point parse_job_posting
```

### 3. Dify Workflow の構築

`src/dify-workflows/` 内の設計ドキュメントに従ってワークフローを構築

### 4. Make シナリオの設定

`docs/make_integration.md` を参照

## 📊 ロードマップ

- **v1.0**: 1社ずつの手動調査
- **v2.0**: リスト・スクリーニング機能（100社一括処理）
